const toUpperCase = (str) => str.toUpperCase();
const firstLetter = (str) => str.charAt(0);
const concatenate = (strOne, strTwo) => `${strOne} ${strTwo}`;

module.exports = { toUpperCase, firstLetter, concatenate };
